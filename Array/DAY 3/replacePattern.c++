#include<iostream>
#include<string>
using namespace std;
int main(){
    string s;
    getline(cin,s);
    string pattern;
    getline(cin,pattern);
    string temp;
    getline(cin,temp);
    char replacementCharacter=temp[0];
    while(word.find(pattern)!=string::npos){
        int pos=word.find(pattern);
        word.replace()
    }
}